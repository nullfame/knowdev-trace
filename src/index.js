// * Vue plugins call the install function to install themselves into Vue

import getBrowserId from "./functions/getBrowserId.function.js";
import install from "./install.function.js";

export default {
  getBrowserId,
  install,
};
